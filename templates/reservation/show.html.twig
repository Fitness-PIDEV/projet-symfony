{% extends 'base-front.html.twig' %}
{% block content %}

{% block title %}Reservation{% endblock %}

{% block body %}
    <center>
        <div class="container">
            <div class="col-md-9">
                <ul class="breadcrumb">
                    <li><a href="#">Home</a></li>
                    <li>"Reservation"</li>
                </ul>
            </div>
            <div class="col-md-9">
            <div class="box">
                <div class="table-responsive">
    <h1>Reservation</h1>

    <table class="table">
        <tbody>

            <tr>
                <th>Dispo</th>
                <td>{{ reservation.dispo }}</td>
            </tr>
            <tr>
                <th>NomCours</th>
                <td>{{ reservation.nomCours }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ reservation.date ? reservation.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>NbrParticipant</th>
                <td>{{ reservation.nbrParticipant }}</td>
            </tr>

            <tr>
                <th>Nom</th>
                <td>{{ reservation.nom }}</td>
            </tr>
        </tbody>
    </table>
                </div>

    <a class="btn btn-primary navbar-btn" href="{{ path('app_reservation_index') }}">back to list</a>

    <a class="btn btn-primary navbar-btn" href="{{ path('app_reservation_edit', {'id': reservation.id}) }}">edit</a>

    {{ include('reservation/_delete_form.html.twig') }}
            </div>
            </div>
        </div>
    </center>
{% endblock %}
{% endblock %}
